import { Routes } from '@angular/router';
import { TemperatureComponent } from './modules/temperature/temperature.component';

export const routes: Routes = [
  {
    path: '', component: TemperatureComponent, title: ''
  }
];
